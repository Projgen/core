import type { PatchJsonStep } from "@/template-engine/domain";
import type { VariableValue } from "@/template-engine/domain/variable-value";

export const executePatchJsonStep = async (
  step: PatchJsonStep,
  variables: VariableValue[],
) => {};
